<?php
defined('BASEPATH') or exit('No direct script access allowed');

class Data extends CI_Model
{
    // public function checkleveluser($email, $password)
    // {
    //     if ($email && $password) {
    //         $sql = "SELECT * FROM users WHERE email = '$email' AND password = '$password' AND id_level = 1 "
    //     }
    // }
}
